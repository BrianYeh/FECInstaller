<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"

    tools:context="installer.com.firich.fecinstaller.MainActivity">


    <LinearLayout
        android:id="@+id/linearLayout_install"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@drawable/customborder">
        <Button
            android:layout_width="300dp"
            android:layout_height="match_parent"
            android:background="@drawable/button_style"
            android:text="Install factory tool."
            android:layout_weight="8"
            android:textSize="24sp"
            android:id="@+id/install_factory_tool"
            android:onClick="install_factory_tool_click" />
        <TextView
            android:id="@+id/textViewInstall"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            android:background="@drawable/customborder"
            android:layout_weight="2"
            android:textSize="24sp"
            android:text="Not yet." />
    </LinearLayout>
</RelativeLayout>
